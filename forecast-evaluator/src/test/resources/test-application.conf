database {
  connectionString = "jdbc:h2:mem:testdb;MODE=Oracle;DB_CLOSE_DELAY=-1"
  username = "test"
  password = "test"
}

validator {
  intervalMinutes = 5
  dailyCompletenessThreshold = 20
  batchSize = 100
}